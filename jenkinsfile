pipeline {
   agent any
   stages {
      stage('clone repo') {
                        steps {
                           git 'https://github.com/gitbablu/jenkins-and-ansible.git'
                              }
                          }
       stage('run ansible playbook') {
                          steps {

                                 }
                           }
}
}
 
